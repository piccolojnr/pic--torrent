<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= description.name %> - Details | Pico - Torrent</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="container">
      <header class="site-header">
        <nav class="nav-wrapper">
          <a href="/" class="logo">Pico Torrent</a>
        </nav>
      </header>

      <main class="details-content">
        <div class="details-header card">
          <h1><%= description.name %></h1>
          <div class="details-actions">
            <%- print_magnet_button(description.info_hash, description.name) %>
          </div>
        </div>

        <div class="details-grid">
          <div class="info-card card">
            <h2>Information</h2>
            <div class="info-grid">
              <div class="info-item">
                <span class="info-label">Category</span>
                <span class="info-value"><%- print_category(description.category) %></span>
              </div>
              <div class="info-item">
                <span class="info-label">Files</span>
                <span class="info-value"><%= description.num_files %></span>
              </div>
              <div class="info-item">
                <span class="info-label">Size</span>
                <span class="info-value"><%= print_size(description.size) %></span>
              </div>
              <div class="info-item">
                <span class="info-label">Added</span>
                <span class="info-value"><%= print_date(description.added) %></span>
              </div>
              <div class="info-item">
                <span class="info-label">Uploader</span>
                <span class="info-value"><%- print_username(description.username) %></span>
              </div>
              <div class="info-item">
                <span class="info-label">Seeds</span>
                <span class="info-value seeds"><%= description.seeders %></span>
              </div>
              <div class="info-item">
                <span class="info-label">Peers</span>
                <span class="info-value peers"><%= description.leechers %></span>
              </div>
            </div>
          </div>

          <% if (description.descr) { %>
          <div class="description-card card">
            <h2>Description</h2>
            <div class="description-content">
              <pre><%= description.descr %></pre>
            </div>
          </div>
          <% } %>

          <div class="files-card card">
            <h2>Files (<%= files.length %>)</h2>
            <div class="files-list">
              <% files.forEach(file => { %>
                <div class="file-item">
                  <div class="file-name"><%= file.name[0] %></div>
                  <div class="file-size"><%= print_size(file.size[0]) %></div>
                </div>
              <% }) %>
            </div>
          </div>
        </div>
      </main>

      <footer class="site-footer">
        <a href="javascript:history.back()" class="back-link">
          <svg viewBox="0 0 24 24" width="16" height="16">
            <path d="M20 11H7.83l5.59-5.59L12 4l-8 8 8 8 1.41-1.41L7.83 13H20v-2z"/>
          </svg>
          Back to results
        </a>
      </footer>
    </div>
  </body>
</html>
